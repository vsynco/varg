<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
  <link rel="icon" href="/api/placeholder/32/32" type="image/x-icon">
  <title>Puig Legal Group | Abogados Litigantes</title>
  <style>
    :root {
      --primary: #2B4B6F; /* Azul oscuro profesional */
      --secondary: #E0E0DB; /* Beige claro */
      --dark: #1D2731; /* Gris muy oscuro */
      --light: #ffffff;
      --gray: #f8f9fa;
      --gray-dark: #e2e5e9;
      --text: #333333;
      --text-secondary: #666666;
      --shadow: 0 8px 20px rgba(0,0,0,0.06);
      --shadow-large: 0 15px 30px rgba(0,0,0,0.1);
      --accent: #A52A2A; /* Tono burdeos para acentos */
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Playfair Display', 'Segoe UI', Roboto, 'Helvetica Neue', Arial, serif;
    }

    body {
      color: var(--text);
      line-height: 1.6;
      font-size: 16px;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    @media (max-width: 992px) {
      .container {
        padding: 0 16px;
        max-width: 100%;
      }
    }

    /* PROMO */
    .promo-bar {
      background-color: var(--accent);
      color: var(--light);
      padding: 10px 0;
      text-align: center;
      font-weight: 600;
      font-size: 14px;
      top: 0;
      width: 100%;
      z-index: 110;
    }

    /* HERO */
    .hero-split {
      padding: 80px 0 100px;
      background: linear-gradient(rgba(40, 56, 69, 0.92), rgba(40, 56, 69, 0.92)), url('/static/puig/16.jpg') center/cover;
      color: #ffffff;
    }

    .hero-split-container {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: space-between;
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .hero-split-content {
      flex: 1;
      text-align: left;
      padding-right: 30px;
      max-width: 650px;
    }

    .hero-split-logo {
      margin-bottom: 40px;
    }

    .hero-split-logo img {
      height: 60px;
      width: auto;
      display: block;
      max-width: 100%;
      object-fit: contain;
    }

    .hero-split-heading {
      font-size: 42px;
      line-height: 1.2;
      margin-bottom: 30px;
      font-weight: 700;
      letter-spacing: -0.5px;
    }

    .hero-split-description {
      font-size: 18px;
      margin-bottom: 40px;
      opacity: 0.9;
      line-height: 1.7;
      font-weight: 300;
    }

    .hero-split-form-container {
  width: 340px;
  background-color: #ffffff;
  border-radius: 8px;
  padding: 30px;
  box-shadow: 0 15px 30px rgba(0,0,0,0.1);
  color: #333333;
}

.hero-split-form-title {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: 20px;
  color: var(--dark);
  text-align: center;
}


@media (max-width: 768px) {
  .hero-split-form-container {
    width: 95%; /* Casi todo el ancho en pantallas muy pequeñas */
    padding: 20px 15px;
    margin: 0 auto; /* Centrar el formulario */
  }
  
  .hero-split-form-title {
    font-size: 18px; /* Ligera reducción de tamaño para mejor ajuste */
  }
  
  .hero-split-input {
    padding: 12px 14px; /* Campos de formulario ligeramente más compactos */
  }
}


    .hero-split-input {
      width: 100%;
      padding: 14px 16px;
      border: 1px solid #e0e0e0;
      border-radius: 6px;
      font-size: 15px;
      transition: all 0.3s ease;
      margin-bottom: 15px;
    }

    .hero-split-input:focus {
      outline: none;
      border-color: var(--primary);
      box-shadow: 0 0 0 2px rgba(43, 75, 111, 0.1);
    }

    .hero-split-submit {
      width: 100%;
      background-color: var(--primary);
      color: var(--light);
      padding: 14px;
      border-radius: 6px;
      font-weight: 600;
      font-size: 16px;
      border: none;
      cursor: pointer;
      transition: all 0.3s;
      margin-top: 5px;
    }

    .hero-split-submit:hover {
      background-color: #20395A;
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.06);
    }

    .hero-split-note {
      font-size: 12px;
      color: #666666;
      text-align: center;
      margin-top: 15px;
    }

    /* SERVICIOS */
    .services-section {
      padding: 100px 0;
      background-color: var(--gray);
    }

    .section-title {
      text-align: center;
      font-size: 36px;
      margin-bottom: 20px;
      color: var(--dark);
      font-weight: 700;
      letter-spacing: -0.5px;
    }

    .section-subtitle {
      text-align: center;
      margin-bottom: 60px;
      max-width: 600px;
      margin-left: auto;
      margin-right: auto;
      color: var(--text-secondary);
      font-size: 16px;
      font-weight: 300;
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 30px;
    }

    .service-card {
      background-color: var(--light);
      border-radius: 8px;
      overflow: hidden;
      box-shadow: var(--shadow);
      transition: all 0.3s;
    }

    .service-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-large);
    }

    .service-card-image {
      height: 200px;
      background: var(--dark);
      display: flex;
      align-items: center;
      justify-content: center;
      color: var(--primary);
      font-size: 3rem;
      overflow: hidden;
    }

    .service-card-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      object-position: center;
    }

    .service-card-content {
      padding: 30px;
    }

    .service-card-title {
      font-size: 22px;
      font-weight: 700;
      margin-bottom: 15px;
      color: var(--dark);
    }

    .service-card-text {
      color: var(--text);
      margin-bottom: 0;
      font-size: 15px;
      line-height: 1.7;
      font-weight: 300;
    }

    /* EQUIPO */
    .team-section {
      padding: 100px 0;
      background-color: var(--light);
    }

    .team-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 30px;
      max-width: 1000px;
      margin: 0 auto;
    }

    .team-card {
      background-color: var(--gray);
      border-radius: 8px;
      padding: 30px;
      box-shadow: var(--shadow);
      transition: all 0.3s;
      display: flex;
      flex-direction: row;
      align-items: center;
      text-align: left;
    }

    .team-card:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow-large);
    }

    .team-card-image {
      width: 90px;
      height: 90px;
      border-radius: 50%;
      overflow: hidden;
      margin-right: 25px;
      margin-bottom: 0;
      flex-shrink: 0;
      border: 1px solid var(--primary);
    }

    .team-card-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .team-card-content {
      text-align: left;
    }

    .team-card-name {
      font-size: 22px;
      font-weight: 700;
      margin-bottom: 5px;
      color: var(--dark);
    }

    .team-card-title {
      color: var(--primary);
      margin-bottom: 0;
      font-weight: 600;
      font-size: 15px;
    }

    /* CTA */
    .cta-section {
      padding: 100px 0;
      background: linear-gradient(rgba(40, 56, 69, 0.90), rgba(40, 56, 69, 0.90)), url('/static/puig/18.jpg') center/cover;
      color: #ffffff;
      text-align: center;
    }

    .cta-container {
      max-width: 800px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .cta-logo {
      margin-bottom: 40px;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .cta-logo img {
      height: 45px;
      width: auto;
      display: block;
      max-width: 100%;
      object-fit: contain;
    }

    .cta-heading {
      font-size: 36px;
      margin-bottom: 25px;
      font-weight: 700;
      line-height: 1.3;
    }

    .cta-description {
      margin-bottom: 40px;
      font-size: 18px;
      opacity: 0.9;
      line-height: 1.7;
      font-weight: 300;
    }

    .cta-button {
      background-color: var(--accent);
      color: var(--light);
      padding: 15px 30px;
      border-radius: 4px;
      font-weight: 600;
      font-size: 16px;
      display: inline-block;
      transition: all 0.3s;
      border: none;
      cursor: pointer;
    }

    .cta-button:hover {
      background-color: #8B2222;
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(0,0,0,0.06);
    }

    /* FOOTER */
    .footer {
      background-color: var(--dark);
      color: var(--light);
      padding: 60px 0 30px;
    }

    .footer-container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 20px;
    }

    .footer-grid {
      display: grid;
      grid-template-columns: 2fr 1fr 1fr 1fr;
      gap: 30px;
      margin-bottom: 40px;
    }

    .footer-logo {
      margin-bottom: 20px;
    }

    .footer-logo img {
      height: 40px;
      width: auto;
      max-width: 100%;
      object-fit: contain;
    }

    .footer-description {
      margin-bottom: 25px;
      opacity: 0.8;
    }

    .footer-social {
      display: flex;
      gap: 15px;
    }

    .footer-social-link {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background-color: rgba(255, 255, 255, 0.1);
      color: var(--light);
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s;
      cursor: pointer;
    }

    .footer-social-link:hover {
      background-color: var(--accent);
      transform: translateY(-3px);
    }

    .footer-heading {
      font-size: 18px;
      font-weight: 700;
      margin-bottom: 20px;
      color: var(--light);
    }

    .footer-links {
      list-style: none;
    }

    .footer-links li {
      margin-bottom: 10px;
      color: rgba(255, 255, 255, 0.8);
      cursor: pointer;
    }

    .footer-contact-item {
      display: flex;
      margin-bottom: 15px;
    }

    .footer-contact-icon {
      margin-right: 10px;
      color: var(--accent);
    }

    .footer-contact-text {
      color: rgba(255, 255, 255, 0.8);
    }

    .footer-bottom {
      padding-top: 30px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .footer-copyright {
      color: rgba(255, 255, 255, 0.6);
      font-size: 14px;
    }

    .footer-bottom-links {
      display: flex;
      gap: 20px;
    }

    .footer-bottom-links span {
      color: rgba(255, 255, 255, 0.6);
      font-size: 14px;
      cursor: pointer;
      transition: all 0.3s;
    }

    .footer-bottom-links span:hover {
      color: var(--accent);
    }

    @media (max-width: 992px) {
      .footer-grid {
        grid-template-columns: 1fr 1fr;
      }
    }

    @media (max-width: 768px) {
      .footer-grid {
        grid-template-columns: 1fr;
      }

      .footer-bottom {
        flex-direction: column;
        gap: 15px;
        text-align: center;
      }
    }
  </style>
</head>
<body>
  <!-- PROMO -->
  <div class="promo-bar">
    <div class="container">
      <span>PUIG LEGAL GROUP</span>
    </div>
  </div>

  <!-- HERO -->
  <section class="hero-split">
    <div class="hero-split-container">
      <div class="hero-split-content">
        <div class="hero-split-logo">
          <img src="/static/puig/logo_white.png" alt="Puig Legal Group">
        </div>
        <h1 class="hero-split-heading">Abogados Litigantes</h1>
        <p class="hero-split-description">Representación jurídica especializada con un enfoque estratégico e innovador. Defensa efectiva de sus intereses en todas las materias judiciales.</p>
      </div>
      <div class="hero-split-form-container">
        <h3 class="hero-split-form-title">Consulta Especializada</h3>
        <form id="heroSplitForm">
          <input type="text" id="name" class="hero-split-input" required placeholder="Nombre">
          <input type="email" id="email" class="hero-split-input" required placeholder="Correo electrónico">
          <input type="tel" id="phone" class="hero-split-input" required placeholder="Teléfono">
          <button type="submit" class="hero-split-submit">Solicitar información</button>
        </form>
        <p class="hero-split-note">Un abogado especialista se pondrá en contacto en breve</p>
      </div>
    </div>
  </section>



  





  <style>
  /* SECCIÓN INNOVADORA MEJORADA */
.approach-section {
  padding: 70px 0;
  background-color: var(--gray);
}

.section-title {
  text-align: center;
  font-size: 32px;
  margin-bottom: 15px;
  color: var(--dark);
  font-weight: 700;
}

.section-subtitle {
  text-align: center;
  margin-bottom: 40px;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
  color: var(--text-secondary);
  font-size: 16px;
  font-weight: 300;
}

/* CONTENEDOR PRINCIPAL */
.approach-container {
  max-width: 1200px;
  margin: 0 auto;
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 25px;
}

/* TARJETAS INTEGRADAS */
.approach-card {
  background-color: var(--light);
  border-radius: 8px;
  overflow: hidden;
  box-shadow: var(--shadow);
  transition: all 0.3s;
  display: flex;
  flex-direction: column;
}

.approach-card:hover {
  transform: translateY(-5px);
  box-shadow: var(--shadow-large);
}

/* CABECERA CON IMAGEN */
.card-header {
  height: 110px;
  position: relative;
  overflow: hidden;
}

.card-bg {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}

.card-bg::after {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(29, 39, 55, 0.65);
  z-index: 2;
}

.card-bg img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.card-title-container {
  position: absolute;
  top: 50%;
  right: 20px;
  transform: translateY(-50%);
  z-index: 3;
  text-align: right;
}

.card-title {
  color: white;
  font-size: 24px;
  font-weight: 700;
  text-shadow: 0 2px 5px rgba(0,0,0,0.5);
}

/* LÍNEA DIVISORIA CON INFORMACIÓN DEL SOCIO */
.team-bar {
  background-color: var(--accent);
  height: 42px;
  width: 100%;
  display: flex;
  align-items: center;
  padding: 0 20px;
  position: relative;
}

.team-avatar {
  width: 70px;
  height: 70px;
  border-radius: 50%;
  border: 3px solid white;
  overflow: hidden;
  box-shadow: 0 2px 6px rgba(0,0,0,0.2);
  position: absolute;
  left: 20px;
  top: -14px;
  z-index: 4;
  background-color: white;
}

.team-avatar img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.team-info {
  display: flex;
  align-items: center;
  margin-left: 85px;
  white-space: nowrap; /* Evita que el texto se divida en múltiples líneas */
  overflow: hidden;    /* Oculta el contenido que excede */
  text-overflow: ellipsis; /* Muestra puntos suspensivos si el texto es demasiado largo */
}

.team-name {
  font-size: 16px;
  font-weight: 700;
  color: white;
  margin-right: 8px;
  margin-bottom: 0;
}

.team-position {
  font-size: 13px;
  color: rgba(255, 255, 255, 0.8);
  font-weight: 400;
}

/* CONTENIDO DE LA TARJETA */
.card-content {
  padding: 30px 25px 25px;
}

.card-text {
  color: var(--text);
  font-size: 15px;
  line-height: 1.6;
  font-weight: 300;
  margin: 0;
}

/* RESPONSIVE */
@media (max-width: 992px) {
  .approach-container {
    padding: 0 15px;
  }
  
  .team-info {
    font-size: 14px; /* Reducir ligeramente el tamaño del texto */
  }
  
  .team-name {
    font-size: 15px;
  }
  
  .team-position {
    font-size: 12px;
  }
}

@media (max-width: 768px) {
  .approach-section {
    padding: 50px 0;
  }
  
  .approach-container {
    grid-template-columns: 1fr;
    max-width: 550px;
  }
  
  .card-title {
    font-size: 20px;
  }
  
  .team-bar {
    height: 38px; /* Reducido para mejor proporción en pantallas medianas */
  }
  
  .team-avatar {
    width: 60px;
    height: 60px;
    top: -11px; /* Ajustado para el nuevo tamaño */
  }
  
  .team-info {
    margin-left: 75px; /* Ajustado para el nuevo tamaño de avatar */
  }
}

@media (max-width: 576px) {
  .team-bar {
    height: 36px; /* Más compacto para móviles */
    padding: 0 15px;
  }
  
  .team-avatar {
    width: 54px;
    height: 54px;
    top: -9px;
    left: 15px;
  }
  
  .team-info {
    margin-left: 65px; /* Ajustado para el nuevo tamaño y posición del avatar */
    max-width: calc(100% - 65px); /* Limitar el ancho para evitar desbordes */
  }
  
  .team-name {
    font-size: 14px;
  }
  
  .team-position {
    font-size: 11px;
  }
  
  .card-content {
    padding: 25px 20px 20px; /* Padding reducido para móviles */
  }
  
  .card-title-container {
    right: 15px; /* Ajuste para móviles */
  }
  
  .card-title {
    font-size: 18px; /* Tamaño más pequeño para móviles */
  }
}

/* Ajuste específico para pantallas muy pequeñas */
@media (max-width: 360px) {
  .team-position {
    max-width: 120px; /* Limitar el ancho para que se corte con ... */
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
}
  </style>
  
  <!-- SECCIÓN ENFOQUE INNOVADORA MEJORADA -->
  <section class="approach-section">
    <div class="container">
      <h2 class="section-title">Nuestro Enfoque</h2>
      <p class="section-subtitle">Asesoría legal integral con especialización en litigios judiciales</p>
      
      <div class="approach-container">
        <!-- TARJETA 1: ESPECIALISTAS EN LITIGIOS - CAMILA -->
        <div class="approach-card">
          <div class="card-header">
            <div class="card-bg">
              <img src="/static/puig/9.jpg" alt="Litigios Civiles">
            </div>
            <div class="card-title-container">
              <h3 class="card-title">Especialistas en Litigios</h3>
            </div>
          </div>
          <!-- LÍNEA DIVISORIA CON INFO DEL SOCIO -->
          <div class="team-bar">
            <div class="team-avatar">
              <img src="/static/puig/cam.png" alt="Camila Puig">
            </div>
            <div class="team-info">
              <h4 class="team-name">Camila Puig</h4>
              <div class="team-position">Socia Fundadora, Litigios</div>
            </div>
          </div>
          <div class="card-content">
            <p class="card-text">Representación judicial especializada en juicios civiles, penales y laborales. Litigación estratégica con precisión técnica y años de experiencia.</p>
          </div>
        </div>
        
        <!-- TARJETA 2: INNOVACIÓN JURÍDICA - JUAN PABLO -->
        <div class="approach-card">
          <div class="card-header">
            <div class="card-bg">
              <img src="/static/puig/12.jpg" alt="Innovación Jurídica">
            </div>
            <div class="card-title-container">
              <h3 class="card-title">Innovación Jurídica</h3>
            </div>
          </div>
          <!-- LÍNEA DIVISORIA CON INFO DEL SOCIO -->
          <div class="team-bar">
            <div class="team-avatar">
              <img src="/static/puig/jp.png" alt="Juan Pablo Vargas">
            </div>
            <div class="team-info">
              <h4 class="team-name">Juan Pablo Vargas</h4>
              <div class="team-position">Socio Fundador, Innovación</div>
            </div>
          </div>
          <div class="card-content">
            <p class="card-text">Implementamos tecnología avanzada para responder a necesidades jurídicas especiales y complejas con soluciones modernas y eficientes.</p>
          </div>
        </div>
      </div>
    </div>
  </section>










  
  <style>
    /* CLIENTES */
    .clients-container {
      margin-bottom: 60px;
      margin-top: 60px;
    }
    
    .clients-header {
      text-align: center;
      margin-bottom: 30px;
    }
    
    .clients-title {
      font-size: 20px;
      font-weight: 700;
      color: var(--dark);
      margin-bottom: 10px;
    }
    
    .clients-subtitle {
      color: var(--text-secondary);
      font-size: 15px;
    }
    
    .clients-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: center;
      gap: 25px;
    }
    
    .client-logo {
      height: 70px;
      width: 140px;
      background-color: var(--gray);
      border-radius: 8px;
      padding: 15px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s;
    }
    
    .client-logo:hover {
      transform: translateY(-5px);
      box-shadow: var(--shadow);
    }
    
    .client-logo img {
      max-width: 100%;
      max-height: 40px;
      filter: grayscale(100%);
      opacity: 0.7;
      transition: all 0.3s;
    }
    
    .client-logo:hover img {
      filter: grayscale(0%);
      opacity: 1;
    }
    
    @media (max-width: 768px) {
      .clients-grid {
        gap: 20px;
      }
      
      .client-logo {
        height: 65px;
        width: 130px;
      }
    }
  </style>
  
  <!-- LOGOS DE CLIENTES -->
  <div class="clients-container">
    <div class="clients-header">
      <h3 class="clients-title">Empresas que confían en nosotros</h3>
      <p class="clients-subtitle">Orgullosos de representar a líderes de diferentes industrias</p>
    </div>
    
    <div class="clients-grid">
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="Agacech">
      </div>
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="Gestión">
      </div>
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="LVK">
      </div>
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="Ivis">
      </div>
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="GrupoAdmin">
      </div>
      <div class="client-logo">
        <img src="/api/placeholder/120/35" alt="Soadcon">
      </div>
    </div>
  </div>




  
  <!-- CTA -->
  <section class="cta-section">
    <div class="cta-container">
      <div class="cta-logo">
        <img src="/static/puig/logo_white.png" alt="Puig Legal Group">
      </div>
      <h2 class="cta-heading">Proteja sus intereses con la mejor defensa jurídica</h2>
      <p class="cta-description">Consulte con nuestros especialistas en litigación y descubra cómo podemos ayudarle a prevenir y resolver sus asuntos jurídicos con estrategias adaptadas a su caso.</p>
      <div class="cta-button" id="agendaButton">Agenda Consulta</div>
    </div>
  </section>

  <!-- FOOTER -->
  <footer class="footer">
    <div class="footer-container">
      <div class="footer-grid">
        <div class="footer-info">
          <div class="footer-logo">
            <img src="/static/puig/logo_white.png" alt="Puig Legal Group">
          </div>
          <p class="footer-description">Puig Legal Group es un estudio jurídico especializado en representación de procesos judiciales, aplicando tecnología avanzada para garantizar transparencia y eficacia en la gestión de sus procesos legales.</p>
          <div class="footer-social">
          </div>
        </div>

        <div class="footer-links-column">
        </div>

        <div class="footer-links-column">
        </div>

        <div class="footer-contact">
          <h3 class="footer-heading">Contacto</h3>
          <div class="footer-contact-item">
            <div class="footer-contact-icon">D</div>
            <div class="footer-contact-text">Av. Presidente Kennedy 5600, Vitacura</div>
          </div>
          <div class="footer-contact-item">
            <div class="footer-contact-icon">T</div>
            <div class="footer-contact-text">+56 9 9827 9867</div>
          </div>
          <div class="footer-contact-item">
            <div class="footer-contact-icon">C</div>
            <div class="footer-contact-text">contacto@puigabogados.cl</div>
          </div>
        </div>
      </div>
      <div class="footer-bottom">
        <div class="footer-copyright">© 2025 Puig Legal Group. Todos los derechos reservados.</div>
        <div class="footer-bottom-links">
        </div>
      </div>
    </div>
  </footer>

  <script>
// Script para funcionalidad de contacto
document.addEventListener('DOMContentLoaded', function() {
  // Verificar si el botón existe antes de asignar el evento
  var contactButton = document.getElementById('contactButton');
  if (contactButton) {
    contactButton.addEventListener('click', function() {
      document.getElementById('name').focus();
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
    });
  }
  
  // Asegurarnos de que el botón de agenda funcione correctamente
  var agendaButton = document.getElementById('agendaButton');
  if (agendaButton) {
    agendaButton.addEventListener('click', function() {
      // Desplazar a la parte superior de la página
      window.scrollTo({
        top: 0,
        behavior: 'smooth'
      });
      
      // Opcional: enfocar el campo de nombre después de un breve retraso
      setTimeout(function() {
        var nameField = document.getElementById('name');
        if (nameField) {
          nameField.focus();
        }
      }, 800); // Esperar 800ms para asegurar que el desplazamiento haya terminado
    });
  }
  
  // Manejar el envío del formulario
  var heroForm = document.getElementById('heroSplitForm');
  if (heroForm) {
    heroForm.addEventListener('submit', function(e) {
      e.preventDefault();
      alert('Gracias por su mensaje. Un abogado especialista se pondrá en contacto con usted a la brevedad.');
      this.reset();
    });
  }
});
  </script>
</body>
</html>