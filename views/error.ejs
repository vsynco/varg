<!DOCTYPE html>
<html>
<head>
    <title>Error</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        h1 { color: #D32F2F; }
        .error-container { border: 1px solid #ccc; padding: 20px; border-radius: 5px; }
        .error-details { background: #f8f8f8; padding: 10px; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="error-container">
        <h1>Oops! Ha ocurrido un error</h1>
        <p>Lo sentimos, algo no ha funcionado correctamente.</p>
        
        <div class="error-details">
            <% if (message) { %>
                <p><strong>Mensaje:</strong> <%= message %></p>
            <% } %>
            
            <% if (error && error.status) { %>
                <p><strong>Estado:</strong> <%= error.status %></p>
            <% } %>
            
            <% if (process.env.NODE_ENV === 'development' && error && error.stack) { %>
                <h3>Stack Trace:</h3>
                <pre><%= error.stack %></pre>
            <% } %>
        </div>
        
    </div>
</body>
</html>